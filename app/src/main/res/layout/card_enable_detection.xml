<?xml version="1.0" encoding="utf-8"?>
<!-- One-time CTA shown above the "Now playing" header until the user
     grants Notification access. The disclosure copy is deliberate: we
     need the listener bind only for the MEDIA_CONTENT_CONTROL
     privilege it grants — we never read notifications. The
     accompanying service (PlaybackListenerService) declares
     `disabled_filter_types` covering every category for defence in
     depth. -->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/enableDetectionCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="?attr/colorOutline"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <TextView
            android:id="@+id/enableDetectionTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Session detection"
            android:textAppearance="@style/TextAppearance.Material3.TitleSmall"
            android:textColor="?attr/colorOnSurface" />

        <TextView
            android:id="@+id/enableDetectionBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="To list YouTube, Netflix, Chrome, games etc., grant Notification access. We never read your notifications — this is the only Android API that exposes active media sessions to apps."
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/enableDetectionButton"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Enable detection" />
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
